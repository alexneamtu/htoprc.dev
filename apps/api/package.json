{
  "name": "@htoprc/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "db:migrate": "wrangler d1 migrations apply htoprc-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply htoprc-db --remote",
    "db:seed": "wrangler d1 execute htoprc-db --local --file=./src/db/seed.sql"
  },
  "dependencies": {
    "@htoprc/parser": "workspace:*",
    "hono": "^4.11.0",
    "graphql": "^16.10.0",
    "graphql-yoga": "^5.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.105.0"
  }
}
