name: Release

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          fetch-depth: 0

      - name: Get latest tag
        id: latest_tag
        run: |
          latest=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "tag=$latest" >> $GITHUB_OUTPUT

      - name: Calculate next version
        id: next_version
        run: |
          current="${{ steps.latest_tag.outputs.tag }}"
          # Remove 'v' prefix
          version="${current#v}"

          # Split into parts
          IFS='.' read -r major minor patch <<< "$version"

          # Get commit messages since last tag
          if [ "$current" = "v0.0.0" ]; then
            commits=$(git log --format="%s" --no-merges -20)
          else
            commits=$(git log --format="%s" --no-merges "${current}..HEAD")
          fi

          # Determine version bump based on conventional commits
          bump="patch"
          while IFS= read -r msg; do
            # Check for breaking changes (major bump)
            if echo "$msg" | grep -qE "^[a-z]+(\(.+\))?!:|BREAKING CHANGE"; then
              bump="major"
              break
            fi
            # Check for features (minor bump)
            if echo "$msg" | grep -qE "^feat(\(.+\))?:"; then
              bump="minor"
            fi
          done <<< "$commits"

          echo "Detected bump type: $bump"

          # Apply version bump
          case $bump in
            major)
              major=$((major + 1))
              minor=0
              patch=0
              ;;
            minor)
              minor=$((minor + 1))
              patch=0
              ;;
            patch)
              patch=$((patch + 1))
              ;;
          esac

          next="v${major}.${minor}.${patch}"

          # Ensure tag doesn't already exist (concurrent releases)
          while git rev-parse "$next" >/dev/null 2>&1; do
            echo "Tag $next already exists, incrementing patch..."
            patch=$((patch + 1))
            next="v${major}.${minor}.${patch}"
          done

          echo "version=$next" >> $GITHUB_OUTPUT
          echo "bump=$bump" >> $GITHUB_OUTPUT

      - name: Generate changelog
        id: changelog
        run: |
          latest_tag="${{ steps.latest_tag.outputs.tag }}"

          # Get commits since last tag
          if [ "$latest_tag" = "v0.0.0" ]; then
            commits=$(git log --format="%s" --no-merges -20)
          else
            commits=$(git log --format="%s" --no-merges "${latest_tag}..HEAD")
          fi

          # Check if there are any commits
          if [ -z "$commits" ]; then
            echo "No new commits since $latest_tag"
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "skip=false" >> $GITHUB_OUTPUT

          # Group commits by type
          features=""
          fixes=""
          other=""

          while IFS= read -r msg; do
            if [ -n "$msg" ]; then
              if echo "$msg" | grep -qE "^feat(\(.+\))?:"; then
                features="${features}- ${msg}\n"
              elif echo "$msg" | grep -qE "^fix(\(.+\))?:"; then
                fixes="${fixes}- ${msg}\n"
              else
                other="${other}- ${msg}\n"
              fi
            fi
          done <<< "$commits"

          # Build changelog
          changelog=""
          if [ -n "$features" ]; then
            changelog="${changelog}### Features\n${features}\n"
          fi
          if [ -n "$fixes" ]; then
            changelog="${changelog}### Bug Fixes\n${fixes}\n"
          fi
          if [ -n "$other" ]; then
            changelog="${changelog}### Other Changes\n${other}\n"
          fi

          # Write to file for multiline output
          echo -e "$changelog" > /tmp/changelog.md
          echo "file=/tmp/changelog.md" >> $GITHUB_OUTPUT

      - name: Create Release
        if: steps.changelog.outputs.skip != 'true'
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631 # v2.2.2
        with:
          tag_name: ${{ steps.next_version.outputs.version }}
          name: ${{ steps.next_version.outputs.version }}
          body_path: /tmp/changelog.md
          generate_release_notes: false
        env:
          # Use PAT instead of GITHUB_TOKEN to trigger the deploy workflow
          # (GITHUB_TOKEN actions don't trigger other workflows to prevent infinite loops)
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

      - name: Install pnpm
        if: steps.changelog.outputs.skip != 'true'
        uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4.2.0
        with:
          version: 10.28.0

      - name: Setup Node.js
        if: steps.changelog.outputs.skip != 'true'
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        if: steps.changelog.outputs.skip != 'true'
        run: pnpm install --frozen-lockfile

      - name: Update parser version
        if: steps.changelog.outputs.skip != 'true'
        run: |
          VERSION="${{ steps.next_version.outputs.version }}"
          # Remove 'v' prefix for npm version
          NPM_VERSION="${VERSION#v}"
          cd packages/htoprc-parser
          npm version "$NPM_VERSION" --no-git-tag-version

      - name: Build and publish parser to npm
        if: steps.changelog.outputs.skip != 'true'
        working-directory: packages/htoprc-parser
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          pnpm run build
          pnpm run test:run
          pnpm publish --access public --no-git-checks

      - name: Release Summary
        if: steps.changelog.outputs.skip != 'true'
        run: |
          echo "## Release Created" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Version | ${{ steps.next_version.outputs.version }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Bump Type | ${{ steps.next_version.outputs.bump }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Previous | ${{ steps.latest_tag.outputs.tag }} |" >> $GITHUB_STEP_SUMMARY
          echo "| npm Package | @htoprc/parser@${{ steps.next_version.outputs.version }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Changelog" >> $GITHUB_STEP_SUMMARY
          cat /tmp/changelog.md >> $GITHUB_STEP_SUMMARY

      - name: Skip Summary
        if: steps.changelog.outputs.skip == 'true'
        run: |
          echo "## Release Skipped" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "No new commits since ${{ steps.latest_tag.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
